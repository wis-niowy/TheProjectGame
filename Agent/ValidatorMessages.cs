using System;
using System.Collections.Generic;
using System.Text;

namespace Player
{
    public static class ValidatorMessages
    {
        public const string INVALID_KEEP_ALIVE_INTERVAL = "Invail keep alive interval";
        public const string INVALID_RETRY_JOIN_GAME_INTERVAL = "Invalid retry join game interval";
    }
}
